ARM Platform
1.	source setenv-arm-<platform>.sh <chip_name> (Set Cross Compiler Environment)
(Notes: This shell script may need to adjust, in fact, wpa_supplicant are not dependent on chip)

2. ./build-arm.sh

X86 Platform
1. source libnl-openssl-config.sh
2. ./build-x86
